style(is="custom-style").
    :root {
        /* Primary and accent colors. Also see color.html for more colors.
        */
        --main-color: #DD1F29;
        --black-color: #000000;
        --light-primary-color: rgba(255, 150, 150, 1);
        --default-primary-color: var(--main-color);
        --dark-primary-color: rgba(150, 0, 0, 1);
    }

    paper-input.login-field {
        --default-primary-color: var(--main-color);
        --primary-text-color: var(--black-color);
        --secondary-text-color: var(--black-color);
        --paper-input-container-label: {
            font-size: 20px;
        };
        --paper-input-container-input: {
            font-size: 20px;
        }
    }

    paper-dialog#alert {
        --paper-dialog-button-color: var(--main-color);
    }

    paper-dialog#alert {
        --paper-dialog-button-color: #DD1F29;
    }
